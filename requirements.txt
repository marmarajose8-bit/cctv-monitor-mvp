# --- Captura de pantalla y visión local ---
mss==9.0.1
opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==1.26.4

# --- Reconocimiento facial ---
# face_recognition depende de dlib (requiere CMake y un compilador C++ instalado
# en el sistema antes de poder instalarse). Ver notas de instalación en README.md
dlib==19.24.6
face_recognition==1.3.0

# --- Firebase ---
firebase-admin==6.5.0

# --- Gemini API ---
google-generativeai==0.8.3

# --- Configuración y utilidades ---
python-dotenv==1.0.1
